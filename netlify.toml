# Netlify deployment configuration for KGOS 2031
[build]
  publish = "dist"
  command = "npm run build"

# Configure redirects for Single Page Application (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
